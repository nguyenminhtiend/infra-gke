apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: service-a
  labels:
    app: service-a
spec:
  selector:
    matchLabels:
      app: service-a
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
